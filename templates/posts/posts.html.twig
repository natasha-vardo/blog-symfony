{% extends 'base.html.twig' %}

{% block title %}Posts list{% endblock %}
{% block body %}

    <table class="lux">
        <thead>
        <tr>
            <td>Author</td>
            <td>Posts</td>
        </tr>
        </thead>
        {% for posts in post %}
            <tr>
                <td>{{ posts.id}}</td>
                <td>{{ posts.title }}</td>
            </tr>
        {% endfor %}
    </table>
    {# wow!#}

{% endblock %}