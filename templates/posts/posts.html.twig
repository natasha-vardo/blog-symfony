{% extends 'base.html.twig' %}

{% block title %}Posts list{% endblock %}
{% block body %}

    <h1>Posts</h1>
    {% for posts in post %}
        Author:{{ posts.author.username }}<br>
        <a href="{{ path('post_one', {'id': posts.id })}}">Post: {{ posts.title }}</a><br>
        {{ posts.description }}<br>
        {{ posts.created| date("d.m.Y") }}<br>
        <hr>

    {% endfor %}

{% endblock %}